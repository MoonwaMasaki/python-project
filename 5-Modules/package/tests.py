from package.math import *
from package.subpackages.mult import multiply


print(addition(5, 10))  # Using the addition function from the math module in the package.
print(subtraction(10, 5))  # Using the subtraction function from the math module
print(multiply(5, 10))  # Using the multiply function from the subpackages.mult module
